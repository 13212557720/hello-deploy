# Docker mapped host binding and port
APP_BIND=127.0.0.1
HOST_PORT=3000

# Application runtime configuration
APP_ENV=development
APP_MESSAGE=Hello from local
APP_VERSION=dev

# Demo secret for deployment practice only
DEMO_SECRET=replace-me
